# Build output
dist/
node_modules/
coverage/

# Vendored third-party libraries are shipped verbatim (and, for minified
# builds, with their upstream source map). Reformatting them changes the
# code Prettier/ESLint autofixes touch — `==` -> `===`, `var` -> `let`/`const`
# — which has broken a minified bundle at runtime before. Mirrors the
# ignorePatterns in .eslintrc.js.
src/assets/**/vendor/**
**/*.min.js

# Auto-generated by scripts/smil-to-waapi.js
src/assets/frontend/src/loading-icons-waapi.js
