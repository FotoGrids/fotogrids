/**
 * Featured Item layout has no frontend JS of its own.
 *
 * Every item is a normal decorated .fg-item that runs the gallery's own
 * click behaviour, and the auto "Show all" button is wired by the
 * LightboxGrid module (lightbox/grid/lightbox-grid.js). This file is
 * intentionally empty; it is not a webpack entry and ships nothing.
 */
