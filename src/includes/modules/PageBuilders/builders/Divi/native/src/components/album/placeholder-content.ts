import { __ } from '@wordpress/i18n';

export const placeholderContent = {
  album: {
    innerContent: { desktop: { value: { albumId: '' } } },
  },
  previewClickBehavior: {
    innerContent: { desktop: { value: { enabled: 'off' } } },
  },
  previewPagination: {
    innerContent: { desktop: { value: { enabled: 'off' } } },
  },
};

export const moduleTitle = __('FotoGrids Album', 'fotogrids');
