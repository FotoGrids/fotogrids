node_modules/

# Webpack build output (regenerated by the `divi-native` config; bundle.css is
# also mirrored byte-for-byte to vb-bundle.css). index.php silence files in
# these dirs stay tracked because the globs below only match the build assets.
build/*.js
build/*.js.map
styles/*.css
styles/*.css.map
