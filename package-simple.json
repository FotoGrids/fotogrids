{
  "name": "fotogrids",
  "version": "0.1.0",
  "description": "FotoGrids WordPress gallery plugin",
  "scripts": {
    "build-simple": "node build-simple.js",
    "zip": "cd dist && zip -r ../fotogrids-v0.1.0.zip . -x '*.map'"
  },
  "devDependencies": {
    "fs-extra": "^11.1.1"
  }
}
